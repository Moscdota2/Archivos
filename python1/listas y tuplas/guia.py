# Guia para entender listas y tuplas
# Lista puede almacenar archivos, puede definirse así:
# a = [] (Esto sería una lista); tambien puede ser así pero no se recomienda: a = list()
# Tupla es casi igual, pero no jajaja, se define así:
# a = (); ó a = tuple()